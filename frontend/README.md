# Python Learner Flutter App

```bash
flutter pub get
flutter run
```

للتشغيل على جهاز حقيقي، مرر عنوان الخادم:

```bash
flutter run --dart-define=API_URL=http://YOUR_COMPUTER_IP:8000/api
```

على Android Emulator يستخدم التطبيق `10.0.2.2` تلقائياً للوصول إلى localhost.
